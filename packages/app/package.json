{
  "name": "@rayhealth/app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --composite false",
    "dev": "tsx watch src/server.ts",
    "lint": "eslint . --ext ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "supertest": "^7.2.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/supertest": "^7.2.0"
  }
}
